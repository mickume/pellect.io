<% if bookmark.time_to_expiration < 16 %>
<div class='bookmark-expired'>
<% elsif bookmark.view_count == 0 %>
<div class='bookmark-unread'>
<% else %>
<div class='bookmark'>
<% end %>
	
	<div class='bookmark-favicon'>
		<%= favicon_tag bookmark.site_id %>
	</div>


	<div class='bookmark-meta'>
		<div class='bookmark-title'>
			<%= link_to bookmark.description, "/r/#{bookmark.url_shortened}", :target => '_blank' %>
		</div>
		<div class='bookmark-site'>
			<%= bookmarks_by_site bookmark.site_id %> . Added <%= timeago(bookmark.created_at) %> 
			<% if !bookmark.archived %>
				. <%= bookmark.time_to_expiration %> days left
			<% end %>
		</div>
	</div>
	
	<div class='bookmark-meta2'>
		<div class='bookmark-controls-top'>
			
		</div>
		<div class='bookmark-controls-bottom'>
			<% if !bookmark.archived %>
				<%= link_to("archive", "/a/#{bookmark.id}", :method => :post) %>
			<% end %>
			<%= link_to 'edit', edit_bookmark_path(bookmark) %>
			<%= link_to 'delete', bookmark, method: :delete %>
		</div>
	</div>

</div>